# ignore vscode config folder and python cache files
.vscode/
__pycache__/

# ignore any file or directory that begins with zz_ (this is my personal convention for temporary files)
zz_*/
zz_*

# ignore training as sometimes these files can be large (this can be changed later if needed)
training_data/

# ignore files that are generated by training a model
mlflow_data_storage/
model_and_metrics_saves/

# ignore files that are generated by running the grid search
grid_search_files/parameter_grid_search_status.txt
grid_search_files/parameter_grid.csv